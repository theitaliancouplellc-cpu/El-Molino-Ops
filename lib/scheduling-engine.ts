export type ScheduleSeverity='error'|'warning'|'info';
export type OptimizationMode='balanced'|'lowest_cost'|'equal_hours'|'preferences';
export type ScheduleEmployee={id:string;full_name:string;active:boolean};
export type ScheduleRole={id:string;name:string};
export type RoleAssignment={employee_id:string;role_id:string;skill_level?:number};
export type AvailabilityRow={employee_id:string;day_of_week:number;available_from:string|null;available_to:string|null;unavailable:boolean};
export type TemporaryAvailabilityRow=AvailabilityRow&{effective_from:string;effective_to:string};
export type TimeOffRow={employee_id:string;starts_on:string;ends_on:string;status:string;category?:'unpaid'|'paid'|'paid_sick';full_day?:boolean;starts_at_time?:string|null;ends_at_time?:string|null};
export type DailyForecast={business_date:string;projected_sales:number;source?:string;confidence?:number|null};
export type ScheduleProfile={employee_id:string;min_weekly_hours:number;target_weekly_hours:number|null;max_weekly_hours:number|null;max_shift_hours:number;min_rest_hours:number;max_consecutive_days:number;hourly_rate:number|null;avoid_overtime:boolean;preferred_days_off:number[];preferred_start:string|null;preferred_end:string|null;allow_split_shifts?:boolean;min_split_gap_hours?:number};
export type CoverageRequirement={id:string;name:string;day_of_week:number;role_id:string;starts_at:string;ends_at:string;min_staff:number;target_staff:number;max_staff:number|null;break_minutes:number;priority:number;shift_type:'opening'|'mid'|'closing'|'other';active:boolean;effective_from:string|null;effective_to:string|null;demand_scalable?:boolean;department?:'foh'|'boh'|'management'|'other';end_mode?:'fixed'|'business_decline';earliest_cut_at?:string|null;template_key?:string|null;required_skill_level?:number};
export type SchedulePeriod={id:string;starts_on:string;ends_on:string;revision:number;status:string};
export type ExistingShift={id:string;employee_id:string|null;role_id:string|null;starts_at:string;ends_at:string;break_minutes:number;status:string;source?:string|null;is_locked?:boolean;coverage_requirement_id?:string|null;schedule_period_id?:string|null};
export type ScheduleEngineSettings={timezone:string;overtime_after_hours:number;mode:'minimum'|'target';preserve_manual_shifts:boolean;overtime_multiplier?:number;optimization_mode?:OptimizationMode;sales_per_labor_hour_target?:number|null};
export type GeneratedShift={employee_id:string|null;role_id:string;starts_at:string;ends_at:string;break_minutes:number;coverage_requirement_id:string;notes:string};
export type ScheduleIssue={severity:ScheduleSeverity;code:string;message:string;date?:string;requirement_id?:string;employee_id?:string;details?:Record<string,unknown>};
export type ScheduleMetrics={requested_slots:number;assigned_slots:number;open_slots:number;coverage_rate:number;scheduled_hours:number;projected_labor_cost:number;projected_overtime_hours:number;employees_scheduled:number;fairness_deviation:number};
export type ScheduleGenerationResult={algorithm_version:'v2.2';shifts:GeneratedShift[];issues:ScheduleIssue[];metrics:ScheduleMetrics;objective_score:number};
export type GenerateScheduleInput={period:SchedulePeriod;employees:ScheduleEmployee[];roles:ScheduleRole[];roleAssignments:RoleAssignment[];availability:AvailabilityRow[];temporaryAvailability?:TemporaryAvailabilityRow[];timeOff:TimeOffRow[];profiles:ScheduleProfile[];requirements:CoverageRequirement[];existingShifts:ExistingShift[];settings:ScheduleEngineSettings;forecasts?:DailyForecast[]};

type Slot={key:string;date:string;requirement:CoverageRequirement;starts_at:string;ends_at:string;hours:number;ordinal:number;desired:number};
type Assigned={employee_id:string;starts_at:string;ends_at:string;hours:number;date:string;shift_type:CoverageRequirement['shift_type'];role_id:string};
type CandidateCheck={ok:boolean;reasons:string[];pendingTimeOff:boolean;softPenalty:number};
type Run={shifts:GeneratedShift[];issues:ScheduleIssue[];objective:number;metrics:ScheduleMetrics};
const ACTIVE_STATUSES=new Set(['scheduled','covered']);
const pad=(n:number)=>String(n).padStart(2,'0');
const round2=(n:number)=>Math.round(n*100)/100;
const num=(v:unknown,fallback=0)=>Number.isFinite(Number(v))?Number(v):fallback;
const clamp=(n:number,min:number,max:number)=>Math.max(min,Math.min(max,n));

export function addDateDays(ymd:string,days:number){const [y,m,d]=ymd.split('-').map(Number);const x=new Date(Date.UTC(y,m-1,d+days,12));return `${x.getUTCFullYear()}-${pad(x.getUTCMonth()+1)}-${pad(x.getUTCDate())}`}
export function dateDayOfWeek(ymd:string){const [y,m,d]=ymd.split('-').map(Number);return new Date(Date.UTC(y,m-1,d,12)).getUTCDay()}
export function timeMinutes(value:string|null|undefined){if(!value)return null;const [h,m]=value.slice(0,5).split(':').map(Number);return Number.isFinite(h)&&Number.isFinite(m)?h*60+m:null}
function zonedParts(at:Date,timeZone:string){const parts=new Intl.DateTimeFormat('en-US',{timeZone,year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',second:'2-digit',hourCycle:'h23'}).formatToParts(at);const get=(t:Intl.DateTimeFormatPartTypes)=>Number(parts.find(p=>p.type===t)?.value||0);return {year:get('year'),month:get('month'),day:get('day'),hour:get('hour'),minute:get('minute'),second:get('second')}}
export function zonedLocalToIso(date:string,time:string,timeZone:string){const [y,m,d]=date.split('-').map(Number),[hh,mm,ss=0]=time.slice(0,8).split(':').map(Number);const localMs=Date.UTC(y,m-1,d,hh,mm,ss);let guess=localMs;for(let i=0;i<3;i++){const p=zonedParts(new Date(guess),timeZone),represented=Date.UTC(p.year,p.month-1,p.day,p.hour,p.minute,p.second);guess=localMs-(represented-guess)}return new Date(guess).toISOString()}
function localDate(atIso:string,timeZone:string){const p=zonedParts(new Date(atIso),timeZone);return `${p.year}-${pad(p.month)}-${pad(p.day)}`}
export function shiftNetHours(startsAt:string,endsAt:string,breakMinutes=0){return Math.max(0,(new Date(endsAt).getTime()-new Date(startsAt).getTime())/3600000-num(breakMinutes)/60)}
function overlaps(aStart:string,aEnd:string,bStart:string,bEnd:string){return new Date(aStart)<new Date(bEnd)&&new Date(aEnd)>new Date(bStart)}
function covers(shift:ExistingShift,slot:Slot){return shift.role_id===slot.requirement.role_id&&shift.employee_id!==null&&ACTIVE_STATUSES.has(shift.status)&&new Date(shift.starts_at)<=new Date(slot.starts_at)&&new Date(shift.ends_at)>=new Date(slot.ends_at)}
export function minimumCoverageCountForShifts(shifts:ExistingShift[],roleId:string,startsAt:string,endsAt:string){const start=new Date(startsAt).getTime(),end=new Date(endsAt).getTime();const relevant=shifts.filter(s=>s.role_id===roleId&&s.employee_id!==null&&ACTIVE_STATUSES.has(s.status)&&new Date(s.starts_at).getTime()<end&&new Date(s.ends_at).getTime()>start);const boundaries=[start,end,...relevant.flatMap(s=>[Math.max(start,new Date(s.starts_at).getTime()),Math.min(end,new Date(s.ends_at).getTime())])].filter((v,i,a)=>a.indexOf(v)===i).sort((a,b)=>a-b);if(boundaries.length<2)return 0;let minimum=Infinity;for(let i=0;i<boundaries.length-1;i++){const a=boundaries[i],b=boundaries[i+1];if(b<=a)continue;const count=relevant.filter(s=>new Date(s.starts_at).getTime()<=a&&new Date(s.ends_at).getTime()>=b).length;minimum=Math.min(minimum,count)}return Number.isFinite(minimum)?minimum:0}
function inRange(day:string,start:string,end:string){return day>=start&&day<=end}
function timeOffOverlapsSlot(o:TimeOffRow,slot:Slot,input:GenerateScheduleInput){if(o.full_day!==false)return inRange(slot.date,o.starts_on,o.ends_on);if(o.starts_on!==slot.date||o.ends_on!==slot.date||!o.starts_at_time||!o.ends_at_time)return false;return overlaps(slot.starts_at,slot.ends_at,zonedLocalToIso(slot.date,o.starts_at_time,input.settings.timezone),zonedLocalToIso(slot.date,o.ends_at_time,input.settings.timezone))}
function availabilityForSlot(employeeId:string,slot:Slot,input:GenerateScheduleInput){const temporary=(input.temporaryAvailability??[]).filter(a=>a.employee_id===employeeId&&a.day_of_week===slot.requirement.day_of_week&&inRange(slot.date,a.effective_from,a.effective_to)).sort((a,b)=>b.effective_from.localeCompare(a.effective_from))[0];return temporary??input.availability.find(a=>a.employee_id===employeeId&&a.day_of_week===slot.requirement.day_of_week)}
function profileFor(input:GenerateScheduleInput,id:string):Required<ScheduleProfile>{const p=input.profiles.find(x=>x.employee_id===id);return {employee_id:id,min_weekly_hours:p?.min_weekly_hours??0,target_weekly_hours:p?.target_weekly_hours??null,max_weekly_hours:p?.max_weekly_hours??null,max_shift_hours:p?.max_shift_hours??12,min_rest_hours:p?.min_rest_hours??8,max_consecutive_days:p?.max_consecutive_days??6,hourly_rate:p?.hourly_rate??null,avoid_overtime:p?.avoid_overtime??true,preferred_days_off:p?.preferred_days_off??[],preferred_start:p?.preferred_start??null,preferred_end:p?.preferred_end??null,allow_split_shifts:p?.allow_split_shifts??true,min_split_gap_hours:p?.min_split_gap_hours??0.5}}
function assignmentSet(input:GenerateScheduleInput){return new Set(input.roleAssignments.map(a=>`${a.employee_id}:${a.role_id}`))}
function preservedExisting(input:GenerateScheduleInput){return input.existingShifts.filter(s=>ACTIVE_STATUSES.has(s.status)&&s.employee_id&&!(s.source==='auto'&&!s.is_locked&&s.schedule_period_id===input.period.id))}
function assignedFromExisting(input:GenerateScheduleInput):Assigned[]{return preservedExisting(input).map(s=>({employee_id:s.employee_id!,starts_at:s.starts_at,ends_at:s.ends_at,hours:shiftNetHours(s.starts_at,s.ends_at,s.break_minutes),date:localDate(s.starts_at,input.settings.timezone),shift_type:'other',role_id:s.role_id||''}))}
function hoursInPeriod(assignments:Assigned[],employeeId:string,input:GenerateScheduleInput){return assignments.filter(a=>a.employee_id===employeeId&&inRange(a.date,input.period.starts_on,input.period.ends_on)).reduce((n,a)=>n+a.hours,0)}
function consecutiveDaysIfAdded(assignments:Assigned[],employeeId:string,date:string){const days=new Set(assignments.filter(a=>a.employee_id===employeeId).map(a=>a.date));days.add(date);let count=1,d=date;while(days.has(addDateDays(d,-1))){count++;d=addDateDays(d,-1)}d=date;while(days.has(addDateDays(d,1))){count++;d=addDateDays(d,1)}return count}
function activeRequirements(input:GenerateScheduleInput,date:string){const dow=dateDayOfWeek(date);return input.requirements.filter(x=>x.active&&x.day_of_week===dow&&(!x.effective_from||x.effective_from<=date)&&(!x.effective_to||x.effective_to>=date))}
function reqHours(r:CoverageRequirement,date:string,input:GenerateScheduleInput){return shiftNetHours(zonedLocalToIso(date,r.starts_at,input.settings.timezone),zonedLocalToIso(date,r.ends_at,input.settings.timezone),r.break_minutes)}
function demandMultiplier(input:GenerateScheduleInput,date:string){const forecast=input.forecasts?.find(f=>f.business_date===date),splh=input.settings.sales_per_labor_hour_target;if(!forecast||!splh||splh<=0)return null;const reqs=activeRequirements(input,date),fixed=reqs.filter(r=>!r.demand_scalable).reduce((n,r)=>n+r.target_staff*reqHours(r,date,input),0),scalable=reqs.filter(r=>r.demand_scalable).reduce((n,r)=>n+r.target_staff*reqHours(r,date,input),0);if(scalable<=0)return null;const desiredTotal=Math.max(0,forecast.projected_sales/splh);return Math.max(0,(desiredTotal-fixed)/scalable)}
function desiredStaff(input:GenerateScheduleInput,date:string,r:CoverageRequirement){if(input.settings.mode==='minimum')return r.min_staff;let desired=r.target_staff;if(r.demand_scalable){const factor=demandMultiplier(input,date);if(factor!==null){const cap=r.max_staff??r.target_staff;desired=clamp(Math.round(r.target_staff*factor),r.min_staff,cap)}}return desired}
function demandIssues(input:GenerateScheduleInput):ScheduleIssue[]{const issues:ScheduleIssue[]=[];if(input.settings.mode!=='target')return issues;for(let d=input.period.starts_on;d<=input.period.ends_on;d=addDateDays(d,1)){const factor=demandMultiplier(input,d);if(factor===null)continue;const changed=activeRequirements(input,d).filter(r=>r.demand_scalable&&desiredStaff(input,d,r)!==r.target_staff);if(changed.length)issues.push({severity:'info',code:'forecast_scaled_coverage',message:`Sales forecast adjusted ${changed.length} coverage block${changed.length===1?'':'s'} on ${d}.`,date:d,details:{demand_multiplier:round2(factor),projected_sales:input.forecasts?.find(f=>f.business_date===d)?.projected_sales,sales_per_labor_hour_target:input.settings.sales_per_labor_hour_target}})}return issues}
function staticEligibilityReasons(employee:ScheduleEmployee,slot:Slot,input:GenerateScheduleInput,qualified:Set<string>){const reasons:string[]=[];const key=`${employee.id}:${slot.requirement.role_id}`;if(!employee.active)reasons.push('inactive');if(!qualified.has(key))reasons.push('not_qualified');else{const assignment=input.roleAssignments.find(a=>a.employee_id===employee.id&&a.role_id===slot.requirement.role_id);if((assignment?.skill_level??1)<(slot.requirement.required_skill_level??1))reasons.push('insufficient_skill')}if(input.timeOff.some(o=>o.employee_id===employee.id&&o.status==='approved'&&timeOffOverlapsSlot(o,slot,input)))reasons.push('approved_time_off');const av=availabilityForSlot(employee.id,slot,input);if(av){if(av.unavailable)reasons.push('unavailable');else{const from=timeMinutes(av.available_from),to=timeMinutes(av.available_to),st=timeMinutes(slot.requirement.starts_at),en=timeMinutes(slot.requirement.ends_at);if(from===null||to===null||st===null||en===null||st<from||en>to)reasons.push('outside_availability')}}const p=profileFor(input,employee.id);if(slot.hours>p.max_shift_hours+1e-6)reasons.push('max_shift_hours');return reasons}
function gapHours(slot:Slot,a:Assigned){if(new Date(slot.starts_at)>=new Date(a.ends_at))return (new Date(slot.starts_at).getTime()-new Date(a.ends_at).getTime())/3600000;if(new Date(a.starts_at)>=new Date(slot.ends_at))return (new Date(a.starts_at).getTime()-new Date(slot.ends_at).getTime())/3600000;return -1}
function checkCandidate(employee:ScheduleEmployee,slot:Slot,assignments:Assigned[],input:GenerateScheduleInput,qualified:Set<string>):CandidateCheck{const reasons=staticEligibilityReasons(employee,slot,input,qualified),p=profileFor(input,employee.id);for(const a of assignments.filter(x=>x.employee_id===employee.id)){if(overlaps(slot.starts_at,slot.ends_at,a.starts_at,a.ends_at)){reasons.push('overlap');continue}const gap=gapHours(slot,a);if(gap<0)continue;if(a.date===slot.date){if(!p.allow_split_shifts)reasons.push('split_shift_not_allowed');else if(gap<p.min_split_gap_hours)reasons.push('min_split_gap')}else if(p.min_rest_hours>0&&gap<p.min_rest_hours)reasons.push('min_rest')}const h=hoursInPeriod(assignments,employee.id,input);if(p.max_weekly_hours!==null&&h+slot.hours>p.max_weekly_hours+1e-6)reasons.push('max_weekly_hours');if(consecutiveDaysIfAdded(assignments,employee.id,slot.date)>p.max_consecutive_days)reasons.push('max_consecutive_days');const pending=input.timeOff.some(o=>o.employee_id===employee.id&&o.status==='pending'&&timeOffOverlapsSlot(o,slot,input));let softPenalty=0;if(pending)softPenalty+=80;if(p.preferred_days_off.includes(slot.requirement.day_of_week))softPenalty+=24;const prefStart=timeMinutes(p.preferred_start),prefEnd=timeMinutes(p.preferred_end),st=timeMinutes(slot.requirement.starts_at)!,en=timeMinutes(slot.requirement.ends_at)!;if(prefStart!==null&&st<prefStart)softPenalty+=8;if(prefEnd!==null&&en>prefEnd)softPenalty+=8;return {ok:reasons.length===0,reasons:[...new Set(reasons)],pendingTimeOff:pending,softPenalty}}
function baselineEligibleCount(slot:Slot,input:GenerateScheduleInput,qualified:Set<string>){return input.employees.filter(e=>staticEligibilityReasons(e,slot,input,qualified).length===0).length}
function candidateScore(employee:ScheduleEmployee,slot:Slot,assignments:Assigned[],input:GenerateScheduleInput,check:CandidateCheck,fairShare:number){const p=profileFor(input,employee.id),hours=hoursInPeriod(assignments,employee.id,input),after=hours+slot.hours,target=p.target_weekly_hours,loadBase=Math.max(1,target??fairShare??1),mode=input.settings.optimization_mode??'balanced',ot=input.settings.overtime_after_hours,mult=input.settings.overtime_multiplier??1.5,rate=p.hourly_rate??0,otBefore=Math.max(0,hours-ot),otAfter=Math.max(0,after-ot),incrementalCost=slot.hours*rate+(otAfter-otBefore)*rate*(mult-1),weights=mode==='lowest_cost'?{load:8,pref:.5,cost:.35,ot:28}:mode==='equal_hours'?{load:85,pref:.7,cost:.02,ot:22}:mode==='preferences'?{load:18,pref:3,cost:.03,ot:20}:{load:35,pref:1,cost:.08,ot:22};let score=(hours/loadBase)*weights.load+check.softPenalty*weights.pref+incrementalCost*weights.cost;if(target!==null){if(after>target)score+=(after-target)*(mode==='equal_hours'?7:4);else score-=(Math.min(after,target)-Math.min(hours,target))*(mode==='equal_hours'?1.5:.6)}if(hours<p.min_weekly_hours)score-=Math.min(slot.hours,p.min_weekly_hours-hours)*2;if(p.avoid_overtime&&after>ot)score+=(after-Math.max(hours,ot))*weights.ot;const sameType=assignments.filter(a=>a.employee_id===employee.id&&a.shift_type===slot.requirement.shift_type&&slot.requirement.shift_type!=='other').length;score+=sameType*2.5;if([0,6].includes(slot.requirement.day_of_week))score+=assignments.filter(a=>a.employee_id===employee.id&&[0,6].includes(dateDayOfWeek(a.date))).length*1.5;return score}
function buildSlots(input:GenerateScheduleInput){const slots:Slot[]=[],existing=preservedExisting(input);let ordinal=0;for(let d=input.period.starts_on;d<=input.period.ends_on;d=addDateDays(d,1)){for(const r of activeRequirements(input,d)){const desired=desiredStaff(input,d,r);if(desired<=0)continue;const base:Slot={key:`${d}:${r.id}`,date:d,requirement:r,starts_at:zonedLocalToIso(d,r.starts_at,input.settings.timezone),ends_at:zonedLocalToIso(d,r.ends_at,input.settings.timezone),hours:0,ordinal:ordinal++,desired};base.hours=shiftNetHours(base.starts_at,base.ends_at,r.break_minutes);const already=minimumCoverageCountForShifts(existing,r.role_id,base.starts_at,base.ends_at);for(let i=already;i<desired;i++)slots.push({...base,key:`${base.key}:${i}`})}}return slots}
function failureDetails(slot:Slot,input:GenerateScheduleInput,qualified:Set<string>){const counts:Record<string,number>={};for(const e of input.employees){for(const r of staticEligibilityReasons(e,slot,input,qualified))counts[r]=(counts[r]||0)+1}return counts}
function calculateMetrics(shifts:GeneratedShift[],assignments:Assigned[],input:GenerateScheduleInput,requestedSlots:number):ScheduleMetrics{const generatedAssigned=shifts.filter(s=>s.employee_id),open=shifts.length-generatedAssigned.length,generatedHours=generatedAssigned.reduce((n,s)=>n+shiftNetHours(s.starts_at,s.ends_at,s.break_minutes),0),employees=new Set(generatedAssigned.map(s=>s.employee_id!));let overtime=0,cost=0;const loads:number[]=[];for(const e of input.employees.filter(x=>x.active)){const h=hoursInPeriod(assignments,e.id,input),p=profileFor(input,e.id),ot=Math.max(0,h-input.settings.overtime_after_hours);overtime+=ot;loads.push(h);if(p.hourly_rate!==null)cost+=Math.min(h,input.settings.overtime_after_hours)*p.hourly_rate+ot*p.hourly_rate*(input.settings.overtime_multiplier??1.5)}const mean=loads.length?loads.reduce((a,b)=>a+b,0)/loads.length:0,deviation=loads.length?Math.sqrt(loads.reduce((n,h)=>n+(h-mean)**2,0)/loads.length):0;return {requested_slots:requestedSlots,assigned_slots:generatedAssigned.length,open_slots:open,coverage_rate:requestedSlots?round2((requestedSlots-open)/requestedSlots*100):100,scheduled_hours:round2(generatedHours),projected_labor_cost:round2(cost),projected_overtime_hours:round2(overtime),employees_scheduled:employees.size,fairness_deviation:round2(deviation)}}
function runOrder(slots:Slot[],input:GenerateScheduleInput,order:'constrained'|'priority'|'chronological'):Run{const qualified=assignmentSet(input),assignments=assignedFromExisting(input),shifts:GeneratedShift[]=[],issues:ScheduleIssue[]=[],totalDemand=slots.reduce((n,s)=>n+s.hours,0),active=Math.max(1,input.employees.filter(e=>e.active).length),fairShare=totalDemand/active,sorted=[...slots].sort((a,b)=>{if(order==='constrained'){const ec=baselineEligibleCount(a,input,qualified)-baselineEligibleCount(b,input,qualified);if(ec)return ec}if(order!=='chronological'){const p=b.requirement.priority-a.requirement.priority;if(p)return p}const t=a.starts_at.localeCompare(b.starts_at);return t||a.ordinal-b.ordinal});let softTotal=0;for(const slot of sorted){const candidates=input.employees.map(employee=>({employee,check:checkCandidate(employee,slot,assignments,input,qualified)})).filter(x=>x.check.ok).map(x=>({...x,score:candidateScore(x.employee,slot,assignments,input,x.check,fairShare)})).sort((a,b)=>a.score-b.score||a.employee.full_name.localeCompare(b.employee.full_name)||a.employee.id.localeCompare(b.employee.id)),chosen=candidates[0];if(!chosen){shifts.push({employee_id:null,role_id:slot.requirement.role_id,starts_at:slot.starts_at,ends_at:slot.ends_at,break_minutes:slot.requirement.break_minutes,coverage_requirement_id:slot.requirement.id,notes:`Auto-scheduler could not fill ${slot.requirement.name}.${slot.requirement.end_mode==='business_decline'?' This is a BD shift; the listed end is the safe latest end.':''}`});issues.push({severity:'error',code:'unfilled_coverage',message:`No eligible employee could fill ${slot.requirement.name} on ${slot.date}.`,date:slot.date,requirement_id:slot.requirement.id,details:{eligibility_blocks:failureDetails(slot,input,qualified)}});continue}const e=chosen.employee,p=profileFor(input,e.id),before=hoursInPeriod(assignments,e.id,input),after=before+slot.hours;assignments.push({employee_id:e.id,starts_at:slot.starts_at,ends_at:slot.ends_at,hours:slot.hours,date:slot.date,shift_type:slot.requirement.shift_type,role_id:slot.requirement.role_id});softTotal+=chosen.score;shifts.push({employee_id:e.id,role_id:slot.requirement.role_id,starts_at:slot.starts_at,ends_at:slot.ends_at,break_minutes:slot.requirement.break_minutes,coverage_requirement_id:slot.requirement.id,notes:`Auto-scheduled for ${slot.requirement.name}; qualified and available, projected ${round2(after)} hrs this week.${slot.requirement.end_mode==='business_decline'?' Shift ends at business decline (BD); the listed end time is the safe latest end.':''}`});if(chosen.check.pendingTimeOff)issues.push({severity:'warning',code:'pending_time_off',message:`${e.full_name} has pending time off overlapping this shift.`,date:slot.date,requirement_id:slot.requirement.id,employee_id:e.id});if(p.avoid_overtime&&before<=input.settings.overtime_after_hours&&after>input.settings.overtime_after_hours)issues.push({severity:'warning',code:'overtime_risk',message:`${e.full_name} crosses the overtime threshold in this draft.`,date:slot.date,employee_id:e.id,details:{projected_hours:round2(after)}})}const metrics=calculateMetrics(shifts,assignments,input,slots.length),mode=input.settings.optimization_mode??'balanced',objective=metrics.open_slots*100000+metrics.projected_overtime_hours*1000+metrics.fairness_deviation*(mode==='equal_hours'?80:20)+metrics.projected_labor_cost*(mode==='lowest_cost'?5:.05)+softTotal;return {shifts,issues,metrics,objective}}
export function generateSchedule(input:GenerateScheduleInput):ScheduleGenerationResult{if(input.period.status!=='draft')throw new Error('Auto-scheduling requires a draft schedule period.');if(input.period.ends_on!==addDateDays(input.period.starts_on,6))throw new Error('Schedule period must be exactly seven days.');if(!input.settings.timezone)throw new Error('A restaurant timezone is required.');const slots=buildSlots(input),strategies=(['constrained','priority','chronological'] as const).map(order=>runOrder(slots,input,order));strategies.sort((a,b)=>a.objective-b.objective||a.metrics.open_slots-b.metrics.open_slots);const best=strategies[0],issues=[...demandIssues(input),...best.issues];if(!input.requirements.some(r=>r.active))issues.unshift({severity:'error',code:'no_coverage_requirements',message:'No active coverage requirements are configured, so there is nothing to auto-schedule.'});if(!input.employees.some(e=>e.active))issues.unshift({severity:'error',code:'no_active_employees',message:'No active employees are available for scheduling.'});const qualified=assignmentSet(input);if(input.employees.some(e=>e.active)&&qualified.size===0)issues.unshift({severity:'error',code:'no_role_qualifications',message:'Employees need role qualifications before the auto-scheduler can assign them.'});return {algorithm_version:'v2.2',shifts:best.shifts,issues,metrics:best.metrics,objective_score:round2(best.objective)}}
export function scheduleEmployeeSummary(input:GenerateScheduleInput,result:ScheduleGenerationResult){const existing=assignedFromExisting(input),generated=result.shifts.filter(s=>s.employee_id).map(s=>({employee_id:s.employee_id!,starts_at:s.starts_at,ends_at:s.ends_at,hours:shiftNetHours(s.starts_at,s.ends_at,s.break_minutes),date:localDate(s.starts_at,input.settings.timezone),shift_type:'other' as const,role_id:s.role_id})),all=[...existing,...generated];return input.employees.filter(e=>e.active).map(e=>{const p=profileFor(input,e.id),hours=round2(hoursInPeriod(all,e.id,input));return {employee_id:e.id,full_name:e.full_name,hours,target:p.target_weekly_hours,max:p.max_weekly_hours,overtime:round2(Math.max(0,hours-input.settings.overtime_after_hours))}}).sort((a,b)=>a.full_name.localeCompare(b.full_name))}
