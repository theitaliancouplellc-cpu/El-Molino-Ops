// Deprecated intentionally.
// Ask El Molino is model-first: natural conversation is interpreted by the language model,
// not by hard-coded greeting, continuation, capability, or topic phrase matchers.
export type ConversationHistoryMessage={role:'user'|'assistant';content:string};
