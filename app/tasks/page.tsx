'use client';
import { FormEvent,useEffect,useMemo,useState } from 'react';import { ArrowLeft,CheckCircle2,Circle,Link2,Loader2,MessageSquare,Plus,Trash2,Users } from 'lucide-react';import { supabase } from '@/lib/supabase';import { normalizeRecurrence,recurrenceFrequency,taskIsBlocked } from '@/lib/task-utils';import { dependencyCreatesCycle,isTaskTerminal,toIsoOrNull } from '@/lib/app-hardening';
type Task={id:string;title:string;description:string|null;status:string;priority:string;assigned_employee_id:string|null;assigned_role_id:string|null;due_at:string|null;recurrence_rule:string|null;created_at:string};type Employee={id:string;full_name:string;active:boolean};type Role={id:string;name:string};type Profile={app_role:'admin'|'manager'|'employee';location_id:string|null};type Dependency={task_id:string;depends_on_task_id:string};type Comment={id:string;entity_id:string;author_user_id:string|null;body:string;created_at:string};type Sort='due'|'newest'|'priority';
export default function TasksPage(){const [profile,setProfile]=useState<Profile|null>(null),[tasks,setTasks]=useState<Task[]>([]),[employees,setEmployees]=useState<Employee[]>([]),[roles,setRoles]=useState<Role[]>([]),[dependencies,setDependencies]=useState<Dependency[]>([]),[comments,setComments]=useState<Comment[]>([]),[loading,setLoading]=useState(true),[message,setMessage]=useState(''),[title,setTitle]=useState(''),[description,setDescription]=useState(''),[priority,setPriority]=useState('normal'),[due,setDue]=useState(''),[employee,setEmployee]=useState(''),[role,setRole]=useState(''),[recurrence,setRecurrence]=useState(''),[dependsOn,setDependsOn]=useState(''),[filter,setFilter]=useState<'open'|'all'|'done'>('open'),[sort,setSort]=useState<Sort>('due'),[commentDraft,setCommentDraft]=useState<Record<string,string>>({}),[busy,setBusy]=useState(false);const canManage=profile?.app_role==='admin'||profile?.app_role==='manager';useEffect(()=>{void load()},[]);async function load(){setLoading(true);const {data:u}=await supabase.auth.getUser();if(!u.user){location.href='/';return}const [p,t,e,r,d,c]=await Promise.all([supabase.from('profiles').select('app_role,location_id').eq('id',u.user.id).single(),supabase.from('tasks').select('id,title,description,status,priority,assigned_employee_id,assigned_role_id,due_at,recurrence_rule,created_at').is('deleted_at',null).order('created_at',{ascending:false}).limit(500),supabase.from('employees').select('id,full_name,active').eq('active',true).is('deleted_at',null).order('full_name').limit(500),supabase.from('employee_roles').select('id,name').order('name').limit(200),supabase.from('task_dependencies').select('task_id,depends_on_task_id').limit(5000),supabase.from('comments').select('id,entity_id,author_user_id,body,created_at').eq('entity_type','task').is('deleted_at',null).order('created_at',{ascending:false}).limit(5000)]);if(p.error||t.error)setMessage('Some task data could not be loaded.');setProfile(p.data as Profile);setTasks((t.data??[]) as Task[]);setEmployees((e.data??[]) as Employee[]);setRoles((r.data??[]) as Role[]);setDependencies((d.data??[]) as Dependency[]);setComments([...(c.data??[]) as Comment[]].reverse());setLoading(false)}function flash(v:string){setMessage(v);setTimeout(()=>setMessage(''),4000)}async function create(e:FormEvent){e.preventDefault();if(busy||!profile?.location_id||!title.trim())return;if(due&&!toIsoOrNull(due))return flash('Choose a valid due date and time.');let rr='';try{rr=recurrence?normalizeRecurrence(recurrence):''}catch(err){return flash(err instanceof Error?err.message:'Invalid recurrence.')}setBusy(true);const {data:u}=await supabase.auth.getUser();if(!u.user){setBusy(false);return}const {data,error}=await supabase.from('tasks').insert({location_id:profile.location_id,title:title.trim().slice(0,200),description:description.trim().slice(0,8000)||null,priority,assigned_employee_id:employee||null,assigned_role_id:role||null,due_at:due?toIsoOrNull(due):null,recurrence_rule:rr||null}).select('id').single();if(error){setBusy(false);return flash('Could not create that task.')}if(dependsOn){const dep=await supabase.from('task_dependencies').insert({task_id:data.id,depends_on_task_id:dependsOn});if(dep.error){await supabase.from('tasks').update({deleted_at:new Date().toISOString()}).eq('id',data.id);setBusy(false);return flash('Task was rolled back because its dependency could not be saved.')}}setTitle('');setDescription('');setPriority('normal');setDue('');setEmployee('');setRole('');setRecurrence('');setDependsOn('');setBusy(false);await load();flash('Task created.')}async function toggle(t:Task){if(busy)return;setBusy(true);const done=t.status!=='done',old=tasks;setTasks(xs=>xs.map(x=>x.id===t.id?{...x,status:done?'done':'open'}:x));const {data,error}=await supabase.from('tasks').update({status:done?'done':'open',completed_at:done?new Date().toISOString():null,updated_at:new Date().toISOString()}).eq('id',t.id).eq('status',t.status).select('id').maybeSingle();if(error||!data){setTasks(old);await load();setBusy(false);return flash(error?'Could not update that task.':'That task changed on another device. The latest version has been loaded.')}await load();setBusy(false);if(done&&t.recurrence_rule)flash(`Completed. The next ${recurrenceFrequency(t.recurrence_rule)?.toLowerCase()} recurrence was created automatically.`)}async function addDependency(taskId:string,dependencyId:string){if(busy||!dependencyId)return;if(dependencyCreatesCycle(taskId,dependencyId,dependencies))return flash('That dependency would create a cycle.');if(dependencies.some(d=>d.task_id===taskId&&d.depends_on_task_id===dependencyId))return flash('That dependency already exists.');setBusy(true);const {error}=await supabase.from('task_dependencies').insert({task_id:taskId,depends_on_task_id:dependencyId});if(error){setBusy(false);return flash('Could not add that dependency.')}await load();setBusy(false);flash('Dependency added.')}async function removeDependency(taskId:string,dependencyId:string){if(busy)return;setBusy(true);const {error}=await supabase.from('task_dependencies').delete().eq('task_id',taskId).eq('depends_on_task_id',dependencyId);if(error){setBusy(false);return flash('Could not remove that dependency.')}await load();setBusy(false);flash('Dependency removed.')}async function addComment(taskId:string){const body=(commentDraft[taskId]||'').trim();if(busy||!body||!profile?.location_id)return;setBusy(true);const {data:u}=await supabase.auth.getUser();if(!u.user){setBusy(false);return}const {error}=await supabase.from('comments').insert({location_id:profile.location_id,entity_type:'task',entity_id:taskId,author_user_id:u.user.id,body:body.slice(0,5000)});if(error){setBusy(false);return flash('Could not post that comment.')}setCommentDraft(x=>({...x,[taskId]:''}));await load();setBusy(false)}const statusById=Object.fromEntries(tasks.map(t=>[t.id,t.status]));const rows=useMemo(()=>{const x=tasks.filter(t=>filter==='all'||(filter==='done'?t.status==='done':!isTaskTerminal(t.status))),weight:Record<string,number>={urgent:4,high:3,normal:2,low:1};return [...x].sort((a,b)=>sort==='newest'?new Date(b.created_at).getTime()-new Date(a.created_at).getTime():sort==='priority'?(weight[b.priority]||0)-(weight[a.priority]||0):(a.due_at?new Date(a.due_at).getTime():Number.MAX_SAFE_INTEGER)-(b.due_at?new Date(b.due_at).getTime():Number.MAX_SAFE_INTEGER))},[tasks,filter,sort]);const name=(id:string|null)=>employees.find(e=>e.id===id)?.full_name||null,roleName=(id:string|null)=>roles.find(r=>r.id===id)?.name||null,taskName=(id:string)=>tasks.find(t=>t.id===id)?.title||'Unavailable task';if(loading)return <div className="full-loader"><Loader2 className="spin"/><span>Loading tasks…</span></div>;return <div className="app-shell"><header className="topbar"><a className="round-button" href="/" aria-label="Back"><ArrowLeft/></a><div style={{flex:1}}><div className="brand-title">Task Center</div></div></header><main className="page">{message&&<div className="toast-message">{message}</div>}{canManage&&<div className="card"><form className="form" onSubmit={create}><input className="input" placeholder="Task title" value={title} onChange={e=>setTitle(e.target.value)} maxLength={200} required/><textarea className="textarea" value={description} onChange={e=>setDescription(e.target.value)} maxLength={8000}/><div className="two-col"><select className="select" aria-label="Employee" value={employee} onChange={e=>{setEmployee(e.target.value);if(e.target.value)setRole('')}}><option value="">Unassigned</option>{employees.map(x=><option value={x.id} key={x.id}>{x.full_name}</option>)}</select><select className="select" aria-label="Role" value={role} onChange={e=>{setRole(e.target.value);if(e.target.value)setEmployee('')}}><option value="">No role</option>{roles.map(x=><option value={x.id} key={x.id}>{x.name}</option>)}</select></div><input className="input" aria-label="Due" type="datetime-local" value={due} onChange={e=>setDue(e.target.value)}/><select className="select" value={priority} onChange={e=>setPriority(e.target.value)}><option>low</option><option>normal</option><option>high</option><option>urgent</option></select><select className="select" value={recurrence} onChange={e=>setRecurrence(e.target.value)}><option value="">One time</option><option value="FREQ=DAILY">Daily</option><option value="FREQ=WEEKLY">Weekly</option><option value="FREQ=MONTHLY">Monthly</option></select><select className="select" value={dependsOn} onChange={e=>setDependsOn(e.target.value)}><option value="">No dependency</option>{tasks.filter(t=>!isTaskTerminal(t.status)).map(t=><option key={t.id} value={t.id}>{t.title}</option>)}</select><button className="btn" disabled={busy}><Plus size={16}/> Create assignment</button></form></div>}<div className="task-toolbar"><div className="source-switch">{(['open','all','done'] as const).map(x=><button className={filter===x?'active':''} key={x} onClick={()=>setFilter(x)}>{x}</button>)}</div><select className="select compact-select" value={sort} onChange={e=>setSort(e.target.value as Sort)}><option value="due">Due date</option><option value="priority">Priority</option><option value="newest">Newest</option></select></div>{rows.length?<div className="list">{rows.map(t=>{const deps=dependencies.filter(d=>d.task_id===t.id),blocked=taskIsBlocked(t.id,dependencies,statusById),taskComments=comments.filter(c=>c.entity_id===t.id);return <details className={`list-item ${t.status==='done'?'completed-row':''}`} key={t.id}><summary style={{display:'flex',gap:12,alignItems:'center'}}><button className="task-check" onClick={e=>{e.preventDefault();e.stopPropagation();void toggle(t)}} disabled={busy||(blocked&&t.status!=='done')}>{t.status==='done'?<CheckCircle2/>:<Circle/>}</button><div className="list-main"><b>{t.title}</b><small>{t.due_at?new Date(t.due_at).toLocaleString():'No due date'} · {t.priority}{name(t.assigned_employee_id)?` · ${name(t.assigned_employee_id)}`:roleName(t.assigned_role_id)?` · ${roleName(t.assigned_role_id)}`:''}{blocked?' · BLOCKED':''}</small></div></summary><div className="ops-detail">{deps.map(d=><div className="list-item" key={d.depends_on_task_id}><Link2 size={15}/><div className="list-main"><b>{taskName(d.depends_on_task_id)}</b></div>{canManage&&<button disabled={busy} onClick={()=>removeDependency(t.id,d.depends_on_task_id)}><Trash2 size={14}/></button>}</div>)}{canManage&&<select className="select" disabled={busy} defaultValue="" onChange={e=>{const v=e.target.value;e.currentTarget.value='';void addDependency(t.id,v)}}><option value="">Add dependency…</option>{tasks.filter(x=>x.id!==t.id&&!isTaskTerminal(x.status)&&!deps.some(d=>d.depends_on_task_id===x.id)&&!dependencyCreatesCycle(t.id,x.id,dependencies)).map(x=><option value={x.id} key={x.id}>{x.title}</option>)}</select>}<div><b><MessageSquare size={14}/> Comments</b>{taskComments.map(c=><div className="list-item" key={c.id}><div className="list-main"><b>{c.body}</b></div></div>)}<div className="discussion-composer" style={{position:'static'}}><textarea maxLength={5000} value={commentDraft[t.id]||''} onChange={e=>setCommentDraft(x=>({...x,[t.id]:e.target.value}))}/><button onClick={()=>addComment(t.id)} disabled={busy||!(commentDraft[t.id]||'').trim()}><Plus/></button></div></div></div></details>})}</div>:<div className="empty-state"><Users/><b>No matching assignments</b></div>}</main></div>}
