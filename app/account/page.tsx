'use client';import { FormEvent,useEffect,useState } from 'react';import { ArrowLeft,KeyRound,Loader2,Mail,ShieldCheck,Trash2,UserRound } from 'lucide-react';import { supabase } from '@/lib/supabase';import { normalizedText,strongEnoughPassword,validEmail } from '@/lib/app-hardening';type Profile={full_name:string|null;app_role:string;location_id:string|null};export default function AccountPage(){const [profile,setProfile]=useState<Profile|null>(null),[email,setEmail]=useState(''),[newEmail,setNewEmail]=useState(''),[password,setPassword]=useState(''),[confirmPassword,setConfirmPassword]=useState(''),[message,setMessage]=useState(''),[loading,setLoading]=useState(true),[busy,setBusy]=useState(false);useEffect(()=>{void load()},[]);async function load(){const {data:u}=await supabase.auth.getUser();if(!u.user){location.href='/';return}setEmail(u.user.email||'');setNewEmail(u.user.email||'');const {data,error}=await supabase.from('profiles').select('full_name,app_role,location_id').eq('id',u.user.id).single();if(error)setMessage('Could not load your account profile.');setProfile(data as Profile);setLoading(false)}function flash(v:string){setMessage(v);setTimeout(()=>setMessage(''),4500)}async function changeName(e:FormEvent<HTMLFormElement>){e.preventDefault();if(busy)return;const full_name=normalizedText(new FormData(e.currentTarget).get('name'),120);if(!full_name)return flash('Enter a display name.');setBusy(true);const {data:u}=await supabase.auth.getUser();if(!u.user){setBusy(false);return}const {error}=await supabase.from('profiles').update({full_name}).eq('id',u.user.id);setBusy(false);if(error)return flash('Could not update your name.');await load();flash('Name updated.')}async function changeEmail(e:FormEvent){e.preventDefault();if(busy)return;const next=newEmail.trim().toLowerCase();if(!validEmail(next))return flash('Enter a valid email address.');if(next===email.trim().toLowerCase())return flash('That is already your account email.');setBusy(true);const {error}=await supabase.auth.updateUser({email:next},{emailRedirectTo:`${location.origin}/account`});setBusy(false);flash(error?'Could not start the email change.':'Confirmation links were sent as required.')}async function changePassword(e:FormEvent){e.preventDefault();if(busy)return;if(!strongEnoughPassword(password))return flash('Use at least 10 characters with letters and a number.');if(password!==confirmPassword)return flash('Passwords do not match.');setBusy(true);const {error}=await supabase.auth.updateUser({password});setBusy(false);if(!error){setPassword('');setConfirmPassword('')}flash(error?'Could not change the password.':'Password changed.')}async function resendVerification(){if(busy)return;if(!validEmail(email))return flash('Your current account email is unavailable.');setBusy(true);const {error}=await supabase.auth.resend({type:'signup',email});setBusy(false);flash(error?'Could not resend verification right now.':'Verification email sent.')}async function deleteAccount(){if(busy||!confirm('Record a permanent account deletion request?'))return;if(!profile?.location_id)return flash('Your location profile is unavailable.');const {data:u}=await supabase.auth.getUser();if(!u.user)return;setBusy(true);const existing=await supabase.from('activity_log').select('id').eq('actor_user_id',u.user.id).eq('action','account_deletion_requested').limit(1);if(existing.data?.length){setBusy(false);return flash('A deletion request is already recorded.')}const {error}=await supabase.from('activity_log').insert({location_id:profile.location_id,actor_user_id:u.user.id,action:'account_deletion_requested',entity_type:'profile',entity_id:u.user.id,summary:'User requested account deletion'});setBusy(false);flash(error?'Could not record the deletion request.':'Deletion request recorded. An administrator must complete deletion.')}if(loading)return <div className="full-loader"><Loader2 className="spin"/></div>;return <div className="app-shell"><header className="topbar"><a className="round-button" href="/"><ArrowLeft/></a><div className="brand-title">Account & Security</div></header><main className="page">{message&&<div className="toast-message">{message}</div>}<div className="settings-card"><form className="setting-form" onSubmit={changeName}><div className="settings-icon"><UserRound/></div><div><b>Display name</b><input className="input" name="name" maxLength={120} defaultValue={profile?.full_name||''}/></div><button disabled={busy} className="btn secondary">Save</button></form><div className="setting-form"><ShieldCheck/><div><b>Role</b><span className="status">{profile?.app_role||'unknown'}</span></div></div></div><div className="card"><form className="form" onSubmit={changeEmail}><label>New email</label><input className="input" type="email" autoComplete="email" maxLength={254} value={newEmail} onChange={e=>setNewEmail(e.target.value)} required/><button disabled={busy} className="btn"><Mail/> Change email</button></form><button disabled={busy} className="btn ghost" onClick={resendVerification}>Resend verification email</button></div><div className="card"><form className="form" onSubmit={changePassword}><input className="input" aria-label="New password" type="password" autoComplete="new-password" minLength={10} maxLength={256} value={password} onChange={e=>setPassword(e.target.value)} required/><input className="input" aria-label="Confirm password" type="password" autoComplete="new-password" minLength={10} maxLength={256} value={confirmPassword} onChange={e=>setConfirmPassword(e.target.value)} required/><button disabled={busy} className="btn"><KeyRound/> Change password</button></form></div><div className="card"><button disabled={busy} className="btn" style={{background:'var(--danger)'}} onClick={deleteAccount}><Trash2/> Request account deletion</button></div></main></div>}
