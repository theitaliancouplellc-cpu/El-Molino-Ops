name: Retired primary AI agent upgrade

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This one-time upgrade workflow is retired."
