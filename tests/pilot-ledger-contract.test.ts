import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const s=fs.readFileSync('docs/database/pilot_authoritative_event_ledger_v1.sql','utf8');test('pilot ledger security',()=>{assert.match(s,/enable row level security/i);assert.match(s,/revoke all on public\.pilot_operation_events from anon,authenticated/i);assert.match(s,/current_location_id\(\)/i)});test('pilot critical surfaces',()=>{for(const t of ['time_off_requests','shift_change_requests','schedule_shifts','time_clock_punches','tip_pool_runs'])assert.ok(s.includes(`'${t}'`));assert.match(s,/critical_mutation_success_rate/i)});