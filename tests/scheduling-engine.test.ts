import test from 'node:test';
import assert from 'node:assert/strict';
import {addDateDays,dateDayOfWeek,generateSchedule,GenerateScheduleInput,scheduleEmployeeSummary,zonedLocalToIso} from '../lib/scheduling-engine';

const period={id:'period',starts_on:'2026-08-17',ends_on:'2026-08-23',revision:0,status:'draft'};
const role={id:'server',name:'Server'};
const employees=[{id:'a',full_name:'Alex',active:true},{id:'b',full_name:'Blair',active:true}];
const assignments=[{employee_id:'a',role_id:'server'},{employee_id:'b',role_id:'server'}];
const profile=(id:string,overrides:Record<string,unknown>={})=>({employee_id:id,min_weekly_hours:0,target_weekly_hours:32,max_weekly_hours:40,max_shift_hours:12,min_rest_hours:8,max_consecutive_days:6,hourly_rate:15,avoid_overtime:true,preferred_days_off:[],preferred_start:null,preferred_end:null,allow_split_shifts:true,min_split_gap_hours:.5,...overrides}) as any;
const req=(day:number,overrides:Record<string,unknown>={})=>({id:`r${day}-${String(overrides.name||overrides.id||'x')}`,name:String(overrides.name||'Server coverage'),day_of_week:day,role_id:'server',starts_at:'10:00:00',ends_at:'16:00:00',min_staff:1,target_staff:1,max_staff:null,break_minutes:0,priority:50,shift_type:'mid' as const,active:true,effective_from:null,effective_to:null,demand_scalable:false,...overrides}) as any;
function base(overrides:Partial<GenerateScheduleInput>={}):GenerateScheduleInput{return {period,employees,roles:[role],roleAssignments:assignments,availability:[],timeOff:[],profiles:[profile('a'),profile('b')],requirements:[req(1)],existingShifts:[],settings:{timezone:'America/New_York',overtime_after_hours:40,overtime_multiplier:1.5,mode:'target',preserve_manual_shifts:true,optimization_mode:'balanced',sales_per_labor_hour_target:null},forecasts:[],...overrides}}
const ids=(x:ReturnType<typeof generateSchedule>)=>x.shifts.filter(s=>s.employee_id).map(s=>s.employee_id);

test('date helpers keep restaurant calendar dates stable',()=>{assert.equal(addDateDays('2026-08-17',6),'2026-08-23');assert.equal(dateDayOfWeek('2026-08-17'),1);assert.equal(dateDayOfWeek('2026-08-23'),0)});
test('timezone conversion honors New York daylight and standard time',()=>{assert.equal(zonedLocalToIso('2026-08-17','10:00:00','America/New_York'),'2026-08-17T14:00:00.000Z');assert.equal(zonedLocalToIso('2026-12-07','10:00:00','America/New_York'),'2026-12-07T15:00:00.000Z')});
test('target coverage generates target slots',()=>{const x=generateSchedule(base({requirements:[req(1,{target_staff:2})]}));assert.equal(x.shifts.length,2);assert.equal(x.metrics.assigned_slots,2)});
test('minimum mode generates minimum rather than target staffing',()=>{const input=base({requirements:[req(1,{min_staff:1,target_staff:2})]});input.settings={...input.settings,mode:'minimum'};assert.equal(generateSchedule(input).shifts.length,1)});
test('approved time off is a hard scheduling constraint',()=>{const input=base({employees:[employees[0]],roleAssignments:[assignments[0]],profiles:[profile('a')],timeOff:[{employee_id:'a',starts_on:'2026-08-17',ends_on:'2026-08-17',status:'approved'}]});const x=generateSchedule(input);assert.equal(x.metrics.open_slots,1);assert.ok(x.issues.some(i=>i.code==='unfilled_coverage'))});
test('recurring unavailable day is a hard constraint',()=>{const input=base({employees:[employees[0]],roleAssignments:[assignments[0]],profiles:[profile('a')],availability:[{employee_id:'a',day_of_week:1,available_from:null,available_to:null,unavailable:true}]});assert.equal(generateSchedule(input).metrics.open_slots,1)});
test('availability windows prevent shifts outside employee hours',()=>{const input=base({employees:[employees[0]],roleAssignments:[assignments[0]],profiles:[profile('a')],availability:[{employee_id:'a',day_of_week:1,available_from:'12:00:00',available_to:'18:00:00',unavailable:false}]});const x=generateSchedule(input);assert.equal(x.metrics.open_slots,1);assert.equal((x.issues.find(i=>i.code==='unfilled_coverage')?.details?.eligibility_blocks as any).outside_availability,1)});
test('employees cannot be assigned to unqualified roles',()=>{const input=base({employees:[employees[0]],roleAssignments:[],profiles:[profile('a')]});const x=generateSchedule(input);assert.equal(x.metrics.open_slots,1);assert.ok(x.issues.some(i=>i.code==='no_role_qualifications'))});
test('manual or locked existing coverage is preserved and not duplicated',()=>{const input=base({existingShifts:[{id:'manual',employee_id:'a',role_id:'server',starts_at:'2026-08-17T14:00:00.000Z',ends_at:'2026-08-17T20:00:00.000Z',break_minutes:0,status:'scheduled',source:'manual',is_locked:false,schedule_period_id:'period'}]});const x=generateSchedule(input);assert.equal(x.metrics.requested_slots,0);assert.equal(x.shifts.length,0)});
test('replaceable unlocked auto shifts do not count as preserved coverage',()=>{const input=base({existingShifts:[{id:'old-auto',employee_id:'a',role_id:'server',starts_at:'2026-08-17T14:00:00.000Z',ends_at:'2026-08-17T20:00:00.000Z',break_minutes:0,status:'scheduled',source:'auto',is_locked:false,schedule_period_id:'period'}]});assert.equal(generateSchedule(input).shifts.length,1)});
test('locked auto shifts remain part of the draft',()=>{const input=base({existingShifts:[{id:'locked-auto',employee_id:'a',role_id:'server',starts_at:'2026-08-17T14:00:00.000Z',ends_at:'2026-08-17T20:00:00.000Z',break_minutes:0,status:'scheduled',source:'auto',is_locked:true,schedule_period_id:'period'}]});assert.equal(generateSchedule(input).shifts.length,0)});
test('maximum weekly hours are never exceeded',()=>{const input=base({employees:[employees[0]],roleAssignments:[assignments[0]],profiles:[profile('a',{max_weekly_hours:8,target_weekly_hours:8})],requirements:[req(1),req(2)]});const x=generateSchedule(input);assert.equal(x.metrics.assigned_slots,1);assert.equal(x.metrics.open_slots,1)});
test('maximum shift duration is enforced before scoring',()=>{const input=base({employees:[employees[0]],roleAssignments:[assignments[0]],profiles:[profile('a',{max_shift_hours:4})]});assert.equal(generateSchedule(input).metrics.open_slots,1)});
test('split shifts are supported when employee profile permits them',()=>{const input=base({employees:[employees[0]],roleAssignments:[assignments[0]],profiles:[profile('a',{allow_split_shifts:true,min_split_gap_hours:1})],requirements:[req(1,{id:'am',starts_at:'10:00:00',ends_at:'14:00:00'}),req(1,{id:'pm',starts_at:'16:00:00',ends_at:'20:00:00'})]});assert.equal(generateSchedule(input).metrics.assigned_slots,2)});
test('split shifts can be disabled per employee',()=>{const input=base({employees:[employees[0]],roleAssignments:[assignments[0]],profiles:[profile('a',{allow_split_shifts:false})],requirements:[req(1,{id:'am',starts_at:'10:00:00',ends_at:'14:00:00'}),req(1,{id:'pm',starts_at:'16:00:00',ends_at:'20:00:00'})]});const x=generateSchedule(input);assert.equal(x.metrics.assigned_slots,1);assert.equal(x.metrics.open_slots,1)});
test('minimum split gap is enforced separately from overnight rest',()=>{const input=base({employees:[employees[0]],roleAssignments:[assignments[0]],profiles:[profile('a',{allow_split_shifts:true,min_split_gap_hours:2})],requirements:[req(1,{id:'am',starts_at:'10:00:00',ends_at:'14:00:00'}),req(1,{id:'pm',starts_at:'15:00:00',ends_at:'19:00:00'})]});assert.equal(generateSchedule(input).metrics.assigned_slots,1)});
test('minimum rest prevents unsafe next-day turnaround',()=>{const input=base({employees:[employees[0]],roleAssignments:[assignments[0]],profiles:[profile('a',{min_rest_hours:12,max_weekly_hours:40})],requirements:[req(1,{id:'late',starts_at:'16:00:00',ends_at:'23:00:00'}),req(2,{id:'early',starts_at:'07:00:00',ends_at:'12:00:00'})]});const x=generateSchedule(input);assert.equal(x.metrics.assigned_slots,1);assert.equal(x.metrics.open_slots,1)});
test('maximum consecutive days is enforced',()=>{const seven=[1,2,3,4,5,6,0].map((d,i)=>req(d,{id:`r${i}`,name:`Day ${i}`}));const input=base({employees:[employees[0]],roleAssignments:[assignments[0]],profiles:[profile('a',{max_consecutive_days:6,max_weekly_hours:60,target_weekly_hours:50})],requirements:seven});const x=generateSchedule(input);assert.equal(x.metrics.assigned_slots,6);assert.equal(x.metrics.open_slots,1)});
test('pending time off is avoided when another qualified employee exists',()=>{const input=base({timeOff:[{employee_id:'a',starts_on:'2026-08-17',ends_on:'2026-08-17',status:'pending'}]});assert.deepEqual(ids(generateSchedule(input)),['b'])});
test('pending time off remains a warning rather than silently causing undercoverage',()=>{const input=base({employees:[employees[0]],roleAssignments:[assignments[0]],profiles:[profile('a')],timeOff:[{employee_id:'a',starts_on:'2026-08-17',ends_on:'2026-08-17',status:'pending'}]});const x=generateSchedule(input);assert.equal(x.metrics.assigned_slots,1);assert.ok(x.issues.some(i=>i.code==='pending_time_off'))});
test('preferred day off is a soft preference',()=>{const input=base({profiles:[profile('a',{preferred_days_off:[1]}),profile('b')]});assert.deepEqual(ids(generateSchedule(input)),['b'])});
test('balanced scheduler spreads hours across qualified employees',()=>{const x=generateSchedule(base({requirements:[req(1),req(2),req(3),req(4)]}));const a=ids(x).filter(id=>id==='a').length,b=ids(x).filter(id=>id==='b').length;assert.ok(Math.abs(a-b)<=1)});
test('lowest-cost optimization prefers lower-rate qualified employee',()=>{const input=base({profiles:[profile('a',{hourly_rate:30}),profile('b',{hourly_rate:10})]});input.settings={...input.settings,optimization_mode:'lowest_cost'};assert.deepEqual(ids(generateSchedule(input)),['b'])});
test('equal-hours optimization favors the lower-load employee',()=>{const input=base({profiles:[profile('a'),profile('b')],existingShifts:[{id:'a-load',employee_id:'a',role_id:'server',starts_at:'2026-08-19T14:00:00.000Z',ends_at:'2026-08-19T20:00:00.000Z',break_minutes:0,status:'scheduled',source:'manual'}]});input.settings={...input.settings,optimization_mode:'equal_hours'};assert.deepEqual(ids(generateSchedule(input)),['b'])});
test('sales forecast can scale demand-sensitive coverage up to configured maximum',()=>{const input=base({requirements:[req(1,{target_staff:1,max_staff:3,demand_scalable:true})],forecasts:[{business_date:'2026-08-17',projected_sales:1800}]});input.settings={...input.settings,sales_per_labor_hour_target:100};const x=generateSchedule(input);assert.equal(x.metrics.requested_slots,3);assert.ok(x.issues.some(i=>i.code==='forecast_scaled_coverage'))});
test('sales forecast never scales below minimum staffing',()=>{const input=base({requirements:[req(1,{min_staff:1,target_staff:2,max_staff:3,demand_scalable:true})],forecasts:[{business_date:'2026-08-17',projected_sales:0}]});input.settings={...input.settings,sales_per_labor_hour_target:100};assert.equal(generateSchedule(input).metrics.requested_slots,1)});
test('without forecast data target coverage remains unchanged',()=>{const input=base({requirements:[req(1,{target_staff:2,max_staff:3,demand_scalable:true})]});input.settings={...input.settings,sales_per_labor_hour_target:100};assert.equal(generateSchedule(input).metrics.requested_slots,2)});
test('configured overtime multiplier affects projected labor cost',()=>{const existing=[{id:'load',employee_id:'a',role_id:'server',starts_at:'2026-08-18T12:00:00.000Z',ends_at:'2026-08-20T04:00:00.000Z',break_minutes:0,status:'scheduled',source:'manual'}] as any;const one=base({employees:[employees[0]],roleAssignments:[assignments[0]],profiles:[profile('a',{hourly_rate:20,max_weekly_hours:80,min_rest_hours:0})],existingShifts:existing});one.settings={...one.settings,overtime_multiplier:1.5};const two={...one,settings:{...one.settings,overtime_multiplier:2}};assert.ok(generateSchedule(two).metrics.projected_labor_cost>generateSchedule(one).metrics.projected_labor_cost)});
test('effective coverage dates are respected',()=>{const x=generateSchedule(base({requirements:[req(1,{effective_from:'2026-08-18'})]}));assert.equal(x.shifts.length,0)});
test('inactive employees are excluded',()=>{const input=base({employees:[{...employees[0],active:false},employees[1]]});assert.deepEqual(ids(generateSchedule(input)),['b'])});
test('no active employees produces explicit issue instead of fake success',()=>{const x=generateSchedule(base({employees:employees.map(e=>({...e,active:false}))}));assert.ok(x.issues.some(i=>i.code==='no_active_employees'));assert.equal(x.metrics.open_slots,1)});
test('no coverage configuration is reported explicitly',()=>{const x=generateSchedule(base({requirements:[]}));assert.ok(x.issues.some(i=>i.code==='no_coverage_requirements'));assert.equal(x.shifts.length,0)});
test('schedule generation is deterministic',()=>{const input=base({requirements:[req(1),req(2),req(3)]});assert.deepEqual(generateSchedule(input),generateSchedule(input))});
test('employee summary exposes target, max, hours and overtime',()=>{const input=base({requirements:[req(1),req(2)]}),summary=scheduleEmployeeSummary(input,generateSchedule(input));assert.equal(summary.length,2);assert.ok(summary.every(x=>'hours' in x&&'target' in x&&'max' in x&&'overtime' in x))});
test('published periods cannot be regenerated in browser engine',()=>{assert.throws(()=>generateSchedule(base({period:{...period,status:'published'}})),/draft/)});
test('malformed non-seven-day periods are rejected',()=>{assert.throws(()=>generateSchedule(base({period:{...period,ends_on:'2026-08-22'}})),/seven days/)});
