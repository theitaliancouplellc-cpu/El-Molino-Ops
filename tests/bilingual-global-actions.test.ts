import test from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';const s=readFileSync(new URL('../app/global-actions.tsx',import.meta.url),'utf8');test('command palette localizes system commands while preserving search results and internal navigation safety',()=>{assert.match(s,/useI18n/);assert.match(s,/Paleta de comandos/);assert.match(s,/Centro de operaciones/);assert.match(s,/Bolsa de turnos/);assert.match(s,/safeInternalHref/);assert.match(s,/global_search/);assert.match(s,/\{r\.title\}/);assert.match(s,/r\.subtitle\|\|r\.entity_type/);assert.match(s,/search_text:query,result_limit:20/)});test('command palette keeps keyboard and focus hardening',()=>{assert.match(s,/previousFocus\.current=document\.activeElement/);assert.match(s,/target\?\.focus\?\.\(\)/);assert.match(s,/focusIndex/);assert.match(s,/aria-activedescendant/)});