import test from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';const s=readFileSync(new URL('../app/shift/page.tsx',import.meta.url),'utf8');test('manager shift localizes system chrome while preserving checklist content and mutations',()=>{assert.match(s,/useI18n/);assert.match(s,/Dirigir el turno/);assert.match(s,/\{template\.title\}/);assert.match(s,/item\.checklist_template_items\?\.label/);assert.match(s,/checklist_template_id:template\.id/);assert.match(s,/business_date:today/);assert.match(s,/\.eq\('completed',item\.completed\)\.select\('id'\)\.maybeSingle\(\)/);assert.match(s,/storage_bucket:'el-molino-files'/);assert.match(s,/summary|Checklist photo/)});