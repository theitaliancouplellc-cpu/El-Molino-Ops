import fs from 'node:fs';import path from 'node:path';import test from 'node:test';import assert from 'node:assert/strict';const root=process.cwd();const read=(p:string)=>fs.readFileSync(path.join(root,p),'utf8');
test('schedule compliance localization preserves private data and RPC contracts',()=>{const src=read('app/schedule/compliance/page.tsx');assert.match(src,/useI18n/);assert.match(src,/employee_compliance_profiles/);assert.match(src,/date_of_birth/);assert.match(src,/minor_rules_enabled/);assert.match(src,/schedule_school_calendar_days/);assert.match(src,/schedule_compliance_warnings/);assert.match(src,/upsert_employee_compliance_profile/);assert.match(src,/p_employee_id:id/);assert.match(src,/p_date_of_birth:x\.date_of_birth\|\|null/);assert.match(src,/p_minor_rules_enabled:x\.minor_rules_enabled/);assert.match(src,/p_notes:x\.notes\?\.trim\(\)\|\|null/);assert.match(src,/set_schedule_school_day/);assert.match(src,/p_school_date:date/);assert.match(src,/p_school_in_session:x\.school_in_session/);assert.match(src,/p_school_starts_at:x\.school_in_session\?x\.school_starts_at:null/);assert.match(src,/p_school_ends_at:x\.school_in_session\?x\.school_ends_at:null/);assert.match(src,/p_notes:x\.notes\.trim\(\)\|\|null/)});
