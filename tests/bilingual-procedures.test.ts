import test from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';const s=readFileSync(new URL('../app/procedures/page.tsx',import.meta.url),'utf8');test('procedure builder localizes system chrome while preserving authored procedure data and state tokens',()=>{assert.match(s,/useI18n/);assert.match(s,/Constructor de procedimientos/);assert.match(s,/\{p\.title\}/);assert.match(s,/\{p\.description\}/);assert.match(s,/\{s\.instruction\}/);assert.match(s,/const next=p\.status==='approved'\?'draft':'approved'/);assert.match(s,/\.eq\('version',p\.version\)\.select\('id'\)\.maybeSingle\(\)/);assert.match(s,/create_checklist_from_procedure/);assert.match(s,/p_procedure_id:p\.id,p_title:null,p_period:period/);assert.match(s,/validChecklistPeriod/)});